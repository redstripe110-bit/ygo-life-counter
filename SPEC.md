# 遊戯王ライフ計算アプリ 仕様書 v2

作成日：2026-08-06 / ステータス：**承認待ち（コーディング未着手）**

---

## 0. 確定した前提

| 項目 | 決定 |
|---|---|
| 実装形態 | 単一HTMLのWebアプリ（PWA対応）／`index.html` + `assets/audio/` |
| 音源 | ユーザーが自分で用意して差し替え。ファイル未配置でも動作（無音フォールバック） |
| レイアウト | 上下2分割・**両方とも同じ向き**。**ライフ数字をタップすると電卓が開き、確定すると閉じる** |
| BGM優先 | **BGM4（お互い3999以下）を最優先**。判定は**絶対値** |
| 電卓入力 | 数値入力 → `−` または `+` を押した瞬間に確定（`=` なし） |
| Undo | 両者共通の1本の履歴を、開始時点まで何回でも遡れる |
| ÷2 端数 | **切り上げ**（OCG公式裁定準拠） |
| 初期ライフ | 両者共通の1値 + プリセット（8000 / 4000 / 2000）。端末に保存 |
| ライフ0時 | **LOSE表示なし**。自動終了もしない。0で止まるだけ |
| サイコロ | **6面・1個のみ** |
| 配信 | **GitHub Pages ＋ noindex**（URLを知る人だけが実質使う想定） |

---

## 1. 画面構成

### 1.1 メイン画面（電卓を閉じている状態）

```
┌───────────────────────────┐
│ ⚙設定    🎲アクセサリ   ↩Undo   ⟳リセット │ ← ヘッダー（固定・小）
├───────────────────────────┤
│                                              │
│              PLAYER 1                        │
│                                              │
│              8000                            │ ← 特大。ここをタップ
│                                              │
│           ┌ LOG ┐                            │
├───────────────────────────┤
│                                              │
│              PLAYER 2                        │
│                                              │
│              8000                            │ ← ここをタップ
│                                              │
│           ┌ LOG ┐                            │
└───────────────────────────┘
```

- 上下2分割、**両方とも同じ向き**（1人がスマホを持って両者を管理）。
- ライフ数字は画面幅いっぱいの特大表示（目安 15vw / 最低 72px）。等幅数字（`font-variant-numeric: tabular-nums`）で桁ズレなし。
- **ライフ数字（およびそのプレイヤーの領域全体）をタップすると、そのプレイヤー用の電卓が開く。**
- プレイヤー名はタップで編集（電卓は開かない）。名前は端末に保存。

### 1.2 電卓パネル（ライフをタップして開いた状態）

```
┌───────────────────────────┐
│ ⚙   🎲   ↩   ⟳                              │
├───────────────────────────┤
│              PLAYER 1                        │
│              8000            −1500  ✕        │ ← 対象プレイヤーは
│                                              │   ハイライト表示
├───────────────────────────┤
│   [ 7 ] [ 8 ] [ 9 ]   [  −  ]                │
│   [ 4 ] [ 5 ] [ 6 ]   [  ＋  ]                │
│   [ 1 ] [ 2 ] [ 3 ]   [ ÷2 ]                 │
│   [ 0 ] [ 00 ] [ C ]  [ ✕ 閉じる ]           │ ← ボトムシート
└───────────────────────────┘
```

- 電卓は**ボトムシート**として下からスライドイン（約200ms）。
- 開いている間、**相手側のライフは電卓に隠れてよい**。対象プレイヤーの名前とライフだけは常に見える位置に維持する。
- 対象プレイヤーには枠のハイライト（アクセントカラー）を付け、どちらを操作中か一目で分かるようにする。
- 入力バッファはライフの横に `−1500` のように表示（未入力時は非表示）。
- **確定（`−` `＋` `÷2`）した瞬間にシートを閉じる。** 閉じるアニメーション（約200ms）と並行してライフ変動アニメーション（§2.3）を再生し、**メイン画面上で数字が減っていく様子が見える**ようにする。
- 閉じる操作は4通り：
  1. **`✕ 閉じる` ボタン**（シート右下・常設）
  2. 確定操作（`−` `＋` `÷2`）による自動クローズ
  3. シート外（上の領域）のタップ
  4. 下方向スワイプ ／ 同じプレイヤーのライフを再タップ
- 確定以外で閉じた場合、入力途中のバッファは破棄される（ライフは変わらない）。

### 1.3 共通のUI方針

- 画面は **100dvh 固定・スクロールなし**。iOS Safariのアドレスバー伸縮に耐えるよう `dvh` + `position:fixed`。
- セーフエリアを `env(safe-area-inset-*)` で回避。
- ボタン最小タップ領域 **48×48px**、押下で軽い縮小アニメ＋触覚フィードバック（`navigator.vibrate`、対応端末のみ）。
- ダークテーマ基調（対戦卓で眩しくない）。
- ボタンに `aria-label`、状態を色だけに依存させない。

---

## 2. 電卓ロジック

### 2.1 入力
- 数字ボタンを押すとバッファに追記。**最大5桁**（99999）。それ以上は無視。
- `00` はバッファが空の時は無効（先頭 `00` を防ぐ）。
- 先頭ゼロは自動除去（`0` → `5` と押したら `5`）。
- `C` は入力バッファのクリアのみ。ライフには触れない。

### 2.2 確定
| 操作 | 挙動 |
|---|---|
| `−` | `ライフ = ライフ − バッファ`。バッファが空なら何もしない |
| `＋` | `ライフ = ライフ + バッファ`。バッファが空なら何もしない |
| `÷2` | `ライフ = ceil(ライフ / 2)`（切り上げ）。バッファは無視してクリア |

- 確定するとバッファはクリアされ、**シートは自動的に閉じる**。続けて操作する場合はライフをもう一度タップする。
- **下限0**：0未満にはならず0で止まる。上限99999。
- **ライフ0でも特別な表示・終了処理はしない**（そのまま0を表示。誤操作はUndoで戻せる）。

### 2.3 ライフ変動アニメーション

減った時に「減った」と体感できることを最優先にする。

| 要素 | 内容 |
|---|---|
| 数字のロール | 変動前→変動後まで数値をカウントダウン／アップ（**約600ms**、ease-out）。途中の数値も等幅で滑らかに動く |
| ダメージポップ | ライフの上に `−1500` が赤で浮かび上がり、上へ流れながらフェードアウト（約900ms）。回復時は緑で `+800` |
| 色フラッシュ | 減少時、そのプレイヤー領域の背景が一瞬赤くフラッシュ（約250ms）。回復時は緑 |
| シェイク | 減少時、ライフ数字を左右に微振動（約300ms）。**減少量が大きいほど強く**（〜999：弱 / 1000〜2999：中 / 3000〜：強） |
| 触覚 | 減少時に `navigator.vibrate`（対応端末）。強度は上記3段階に対応 |
| ÷2 | 減少扱い（ロール＋赤フラッシュ＋シェイク）。ポップは `÷2` と実際の減少量を併記 |

- **連続操作対応**：アニメーション中に次の操作（＝ライフを再タップして即確定）が来たら、進行中のロールを即座に終端させて新しい値から開始する。アニメが詰まって遅延することはなく、シートの開閉もアニメ完了を待たない。
- Undo時は**逆再生ではなく、静かに戻す**（ロールのみ・フラッシュとシェイクと音はなし）。
- 端末が「視差を減らす」設定（`prefers-reduced-motion`）の場合、シェイクとフラッシュを無効化し、ロールのみにする。

### 2.4 履歴 / Undo
- 全操作を**両者共通の1本のスタック**で管理。
```js
{ seq, player: 1|2, type: 'sub'|'add'|'half', delta: -1500, before: 8000, after: 6500, at: <ms> }
```
- ヘッダーの `↩` で最新1件を取り消し、ライフを `before` に戻す。**開始時点まで何度でも**。
- Undoした操作はログから消える（Redoなし）。履歴が空のとき `↩` はグレーアウト。
- Undo後もBGM判定は再評価される（ライフが戻ればBGMも戻る）。

### 2.5 ログ表示
- 各プレイヤーの `LOG` ボタン、または電卓シート内から開く。ボトムシート表示。
- **両者混在の時系列（新しい順）**を既定とし、上部タブで `全体 / P1 / P2` を切替。
- 1行の書式：`P1  8000 → 6500  (−1500)` ＋ デュエル開始からの相対時刻 `03:12`。
- 件数上限なし。

### 2.6 リセット
- ヘッダーの `⟳`。**確認ダイアログを1枚挟む**。
- 実行内容：両者のライフを初期ライフに戻す／入力バッファクリア／履歴・ログ全消去／経過時間リセット／BGMをBGM1に戻す／電卓シートを閉じる。
- プレイヤー名と設定は**保持**。

---

## 3. アクセサリ機能

ヘッダーの `🎲` からモーダル（一覧）を開き、「コイントス」「サイコロ」を選択。

### 3.1 コイントス
- ボタンで回転アニメ（約1.2秒）→ **表 / 裏** を表示。`Math.random()` で五分五分。
- 効果音 `coin.mp3`（任意）。
- 直近5回の履歴を小さく表示（`表 裏 表 表 裏`）。「もう一度」で連続実行。

### 3.2 サイコロ
- **6面・1個のみ**。ボタンで転がるアニメ（約1.2秒）→ 出目（1〜6）を大きく表示。
- 効果音 `dice.mp3`（任意）。
- 直近5回の履歴を表示。「もう一度」で連続実行。

> アクセサリの結果はライフログとは別管理（ライフログを汚さない）。

---

## 4. サウンド仕様

### 4.1 ファイル配置

**仮音源をコードで合成して同梱済み**（`.m4a` / **ステレオ・AAC 160kbps**、全7ファイルで約1.6MB）。
すべてオリジナルの手打ちシーケンスで、既存楽曲は一切複製していません。参照としてもらったYouTubeの4曲は
雰囲気の指標としてのみ扱い、音声はダウンロードしていません。

```
assets/audio/
  bgm1.m4a  快活な戦闘曲（イ短調 148BPM）        25.95秒ループ
  bgm2.m4a  少し緊張（156BPM・アルペジオ追加）    24.62秒ループ
  bgm3.m4a  さらに緊張（168BPM・ベース半音上昇）  22.86秒ループ
  bgm4.m4a  ハラハラ（182BPM・減七＋トライトーン）21.10秒ループ
  damage.m4a  ピロピロピロ…ピコン                 1.1秒
  coin.m4a / dice.m4a                             各1秒前後
```
各BGMは16小節（A8＋B8の2部構成）。

- 各トラックは **`.mp3` → `.m4a` の順に探し、先に見つかった方を使う。** 差し替えたい人は同名の `.mp3` を置くだけでよく、
  同梱の `.m4a` を消す必要はない。
- **どちらも無いトラックはスキップ**（エラーで止めない・無音で継続）。
- mp3のエンコーダがこの環境に無いため同梱は `.m4a`（AAC）。iOS Safari / Android Chrome とも対応。
- 遊戯王の公式音源は著作権上使用不可。差し替える場合はフリー素材または自作音源を配置してください。

### 4.2 減少サウンド
- **ライフが減った時のみ**再生（`−` と `÷2`）。加算時は鳴らさない。
- Undoで戻した時は鳴らさない。
- 連打時は前の音を止めて再生（多重再生なし）。

### 4.3 BGM切替ルール

ライフが変化するたびに**両者の現在値から毎回再計算**（回復すれば前のBGMに戻る）。判定は**絶対値**。

| 優先 | 条件 | BGM |
|---|---|---|
| 1 | **P1 ≤ 3999 かつ P2 ≤ 3999** | **BGM4** |
| 2 | P1 ≤ 1999 または P2 ≤ 1999 | BGM3 |
| 3 | P1 ≤ 3999 または P2 ≤ 3999 | BGM2 |
| 4 | 上記以外（開始時含む） | BGM1 |

例：8000/8000→BGM1 ／ 3000/8000→BGM2 ／ 1500/8000→BGM3 ／ 1500/3000→**BGM4** ／ 500/500→**BGM4**

> 絶対値判定で確定。初期ライフを2000などにすると開始直後からBGM4になりますが、その仕様で実装します。

### 4.4 再生の作法

**再生エンジンは Web Audio API。** `<audio loop>` はループ位置がサンプル単位で正確でなく継ぎ目に隙間ができるため、
デコード済みバッファを `AudioBufferSourceNode` の `loop` で回す方式にしている。加えて同梱音源は
**ループ本体の後ろに先頭0.5秒ぶんのコピーを余白として付けて書き出し、アプリ側は既知の正確な尺（`LOOP_SEC`）で
`loopEnd` を指定**する。AACのデコードで末尾が数十ms削られてもループが短くならない。

- BGMはループ再生。切替時は **300msのクロスフェード**（GainNodeでランプ）。同じBGMが継続する場合は再生位置を維持。
- **モバイルの制約**：読み込み直後の自動再生は不可。**最初のタップ**（どのボタンでも可）でオーディオを解放し、BGM1を開始。それまでは無音で、ヘッダーに `🔇 タップで音声ON` を小さく表示。
- ヘッダーにミュートトグル（BGM / SE を個別にON/OFF、設定に保存）。
- 音量：BGM 既定50%、SE 既定80%（設定でスライダー）。

---

## 5. 設定画面

ヘッダーの `⚙` からモーダル。

| 設定項目 | 既定値 | 内容 |
|---|---|---|
| 初期ライフ | 8000 | 数値入力（1〜99999）＋プリセット `8000` `4000` `2000` |
| プレイヤー1 名 | PLAYER 1 | 全角8文字程度まで |
| プレイヤー2 名 | PLAYER 2 | 同上 |
| BGM | ON | ON/OFF ＋ 音量スライダー |
| 効果音 | ON | ON/OFF ＋ 音量スライダー |
| アニメーション | ON | OFFで数字のロールのみになる（端末の低電力設定も自動尊重） |
| 画面スリープ防止 | ON | Wake Lock API（対応端末のみ） |

- 初期ライフを変更して閉じる時、**リセットを実行するか確認**（今のライフを維持して次回から適用も選べる）。
- 設定は `localStorage`（キー接頭辞 `ygolife.`）に保存し、次回起動時に復元。
- **進行中のライフ・ログは保存しない**（起動＝新しいデュエル）。途中復帰は今回スコープ外。

---

## 6. 技術仕様・配信

| 項目 | 内容 |
|---|---|
| 構成 | `index.html`（HTML/CSS/JS 内包）＋ `assets/audio/` ＋ `manifest.json` ＋ `sw.js` ＋ アイコン |
| 依存 | なし（フレームワーク・CDN不使用。完全オフライン動作） |
| 音声 | HTMLAudioElement。ファイル欠損時は例外を握りつぶす |
| 保存 | localStorage |
| PWA | `display: standalone` / `orientation: portrait` / Service Workerでオフラインキャッシュ |
| 対象 | iOS Safari 16+ / Android Chrome 最新。縦持ち前提 |

### 6.1 ファイルツリー
```
ygo-life-counter/
├─ index.html
├─ manifest.json
├─ sw.js
├─ robots.txt          ← noindex 用
├─ icon-192.png / icon-512.png
├─ assets/audio/
│   ├─ README.md       ← 差し替え手順
│   └─ bgm1〜4.mp3 / damage.mp3 / coin.mp3 / dice.mp3
└─ SPEC.md（本書）
```

### 6.2 配信（GitHub Pages ＋ noindex）
- アカウント：**`redstripe110-bit`**（`SM-SmallMountain` とは別の個人アカウント）
- リポジトリ `redstripe110-bit/ygo-life-counter`（**public**）。公開URLは `https://redstripe110-bit.github.io/ygo-life-counter/`
- コミット情報：`user.name = redstripe110-bit` / `user.email = 313723035+redstripe110-bit@users.noreply.github.com`（実メール非公開）。**リポジトリ単位で設定**し、グローバル設定は変更しない
- SSH：専用鍵 `~/.ssh/id_ed25519_ygo` ＋ `~/.ssh/config` のエイリアス `github-ygo`。remote URLは `git@github-ygo:redstripe110-bit/ygo-life-counter.git`（既存の `SM-SmallMountain` 用設定と競合しない）
- 検索除けとして **`<meta name="robots" content="noindex,nofollow">` と `robots.txt` の Disallow** を入れる。
- **正確に言うと：これは「公開URL」です。** URLを知らない人は事実上辿り着けませんが、アクセス制限はかかっていません（リポジトリも公開）。パスワードや認証が必要になったら、Cloudflare Pages ＋ Access への移行を後から行えます。
- 使い方：スマホでURLを開く → Safariの共有メニュー →「ホーム画面に追加」でアプリ化。

---

## 7. スコープ外（やらないこと）

- オンライン対戦・複数端末同期 ／ 3人以上の多人数戦
- デュエル履歴の永続保存・統計 ／ 途中復帰
- ライフ以外のカウンター ／ タイマー・制限時間
- LOSE判定・勝敗記録

いずれも後から追加できる設計にしておきます。

---

## 8. 実装ステップ（承認後）

1. 骨組み：レイアウト＋ライフタップで電卓シート開閉＋テンキー＋増減＋下限0
2. ライフ変動アニメーション（ロール／ポップ／フラッシュ／シェイク／連打対応）
3. 履歴・Undo・ログ・リセット
4. 設定画面 ＋ localStorage 保存
5. サウンド（無音フォールバック込み）＋ BGM切替ロジック
6. アクセサリ（コイントス・サイコロ）
7. PWA化（manifest / sw / アイコン / noindex）＋ 実機幅での表示確認

各ステップ完了ごとにブラウザで動作確認し、スクリーンショットで報告します。
GitHubへの公開は、動作確認が済んで**あなたのゴーが出てから**行います（勝手にpushしません）。

---

## 9. 実装メモ（v3実装時に決めた細部）

仕様書に書ききれていなかった点を、実装時に以下のように判断しました。

| 項目 | 実装した挙動 | 理由 |
|---|---|---|
| 入力バッファの表示 | 符号なしの数値（`1500`）で表示 | `−` か `＋` を押すまで符号が決まらないため、先に `−` を出すと誤解を招く |
| プレイヤーの `LOG` ボタン | そのプレイヤーで絞り込んだ状態で開く（タブで `全体` に切替可） | 押した側の履歴を見たいことがほとんどのため |
| ログの ÷2 | `÷2` バッジを付けて、通常の減算と区別できるようにした | `6000 → 3000 (−3000)` だけでは手動減算と見分けがつかない |
| ミュートボタン | ヘッダーに常設（BGM・SEを一括ON/OFF） | 対戦中に素早く消したい場面があるため。個別調整は設定画面 |
| キーボード操作 | PCでは数字キー / `-` / `+` / `Backspace` / `Esc` が使える | 動作確認とPCブラウザでの利用向けのおまけ |
| Service Worker | HTMLのみネットワーク優先、音源・アイコンはキャッシュ優先 | 更新が即反映されないと差し替え時に混乱するため |
| デバッグ用の口 | `window.__ygo` から状態を読める | 不具合が出た時にコンソールから状況を確認できるように |

### 動作確認済みの内容（375×812 / 320×568 で実機幅シミュレート）

- 8000 − 4001 = 3999 → BGM2、両者3999 → **BGM4**、`1999/3999` でも **BGM4優先** を確認
- `÷2` の切り上げ：3999 → 2000、1999 → 1000
- 下限0クランプ（99999を引いても0で停止）、上限99999
- Undoを開始時点まで連打 → ライフが初期値に戻り、ボタンが自動でグレーアウト。空の状態でさらに押しても壊れない
- 未入力で `−` を押した場合はシートが閉じず、ライフも動かない
- 設定（初期ライフ4000・プレイヤー名）を保存 → リロード後も復元
- 音源ファイルが1つも無い状態で全機能が動作（404は出るがコンソールエラーなし・無音で継続）
- manifest / Service Worker / アイコン / `noindex` / `robots.txt` すべて有効

---

## 10. 変更履歴

### v5（音源の質）
- **ステレオ化**（デチューン2声を左右配置、ハット/アルペジオ/ディレイを広げる）
- 音数を追加：サブベース、コードスタブ、パッド、ピンポンディレイ、ルームリバーブ、
  レイヤードラム（キック3層／スネア4層／オープンハット／クラッシュ／タムフィル）
- ビットレート 112k → **160kbps ステレオ**、中間WAVを16bit → **24bit**
- ドライブと明るさを上げてラウドに（RMS 0.17 → 0.19〜0.21、広がり 0.15 → 0.22〜0.25）

### v4（音源）
- BGMの尺を2倍（8小節 → 16小節、A8＋B8の2部構成）に
- 再生を **Web Audio API** に変更し、ループの継ぎ目の途切れを解消
- 同梱音源に0.5秒のループ余白を付け、`LOOP_SEC` で正確な終端を指定（デコード時の末尾欠けを吸収）
- `file://` で開いた場合はヘッダーにその旨を表示（fetchが使えず音を読み込めないため）

### v3
- **確定した瞬間に電卓シートを閉じる**方式に変更（開きっぱなしをやめた）
- `✕ 閉じる` ボタンをシートに常設（閉じる手段は計4通り）
- 電卓を開いている間、相手のライフは隠れてよいことを明記

### v2（v1からの変更点）
- ライフ数字タップで電卓を開くボトムシート方式に変更（メイン画面はライフ2つだけの大画面表示に）
- ライフ変動アニメーションを §2.3 として詳細化（ロール・ポップ・フラッシュ・シェイク・触覚）
- LOSE表示を削除
- サイコロを6面1個に確定
- BGM判定は絶対値で確定
- 配信方法を GitHub Pages ＋ noindex に確定
