# 音源について

## 同梱している仮音源

このフォルダには、**コードで合成したオリジナルのチップチューン風の仮音源**（`.m4a`）が入っています。
遊戯王やその他の既存楽曲は一切使用・複製していません。方向性の確認用なので、気に入らなければ差し替えてください。

| ファイル | 用途 | 鳴るタイミング | 長さ |
|---|---|---|---|
| `bgm1.m4a` | 快活な戦闘曲 | 両者とも4000以上 | 13.0秒ループ |
| `bgm2.m4a` | 少し緊張 | どちらかが3999以下 | 12.4秒ループ |
| `bgm3.m4a` | あと少し！ さらに緊張 | どちらかが1999以下 | 11.5秒ループ |
| `bgm4.m4a` | お互いギリギリ・ハラハラ | **お互い3999以下（最優先）** | 10.6秒ループ |
| `damage.m4a` | 減少音（ピロピロピロ…ピコン） | ライフが減った時（`−` と `÷2`） | 1.1秒 |
| `coin.m4a` | コイントス | アクセサリのコイン | 1.0秒 |
| `dice.m4a` | サイコロ | アクセサリのダイス | 1.2秒 |

仮音源の作り分け：BGM1はイ短調・148BPMの明るい進行、BGM2は156BPMでアルペジオと半音を足して不穏に、
BGM3は168BPMでベースを半音ずつ押し上げて切迫感を、BGM4は182BPM・減七のアルペジオとトライトーンで
落ち着かない響きにしています。

## 差し替え方

**`.mp3` を同じ名前で置くだけです。** アプリは各トラックについて `.mp3` → `.m4a` の順に探し、
先に見つかった方を使うので、`bgm1.mp3` を置けば同梱の `bgm1.m4a` より優先されます。
`.m4a` の方を消す必要はありません（消しても構いません）。

```
assets/audio/bgm1.mp3   ← これを置けば bgm1.m4a より優先される
```

- 差し替えたいトラックだけ置けばOK。置かなかったものは同梱の仮音源のままです。
- **どちらも無いトラックは単に鳴りません**（エラーにはならず、無音のまま動作します）。
- **BGMはループ再生されます。** ループしても違和感のない素材を選んでください。切替時は300msのクロスフェードがかかります。
- **遊戯王の公式音源（アニメ・ゲームのBGM）は著作権上使用できません。** フリー素材（DOVA-SYNDROME、効果音ラボ など、各サイトの利用規約を確認のうえ）または自作音源を使ってください。

## 知っておくこと

- ブラウザの仕様上、**ページを開いた直後は音が鳴りません。** 画面のどこかを最初にタップした時点で音声が有効になります
  （それまでヘッダーに「🔇 タップで音声ON」と表示されます）。
- 音量はアプリの設定画面（⚙）で BGM / 効果音 それぞれ調整できます。ヘッダーの 🔊 ボタンで一括ミュートもできます。
- 音源を入れ替えても反映されない場合は、Service Worker が古いファイルをキャッシュしています。
  `sw.js` 冒頭の `var CACHE = 'ygolife-v1';` の数字を上げて再デプロイしてください。
